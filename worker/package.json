{
  "name": "line-sticker-gemini-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put VERTEX_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
